# Las unidades systemd y los scripts de shell se ejecutan en Linux:
# forzar LF para que no se rompan al editarse en Windows.
*.sh            text eol=lf
*.service       text eol=lf
*.timer         text eol=lf
*.conf          text eol=lf
